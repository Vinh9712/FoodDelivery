spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: postgres
    password: root

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # ── Kafka (local) ──
  kafka:
    bootstrap-servers: localhost:9092

app:
  jwt:
    secret: aovnaklsnvkasnvkpnapvnakvnksanvkpasnvpasnvpoanpovnakpvnaknvapsvn
    expiration-ms: 86400000

logging:
  level:
    com.fooddelivery.customer: DEBUG
    org.springframework.security: DEBUG
